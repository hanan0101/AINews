# نظرة عامة على التقنية المستخدمة

ملخص بسيط لكيفية عمل النظام — لمشاركته مع GitHub أو عرضه على المدير.

## الفكرة

النظام يبني نشرة إخبارية أسبوعية بالعربي عن تحديثات أدوات الذكاء الاصطناعي، تلقائيًا بالكامل، بدون تدخل بشري في البحث أو الكتابة.

## المراحل الأربع

1. **البحث (Fetching)** — يبحث في الإنترنت عن آخر تحديثات أدوات AI معروفة (ChatGPT، Claude، Cursor... إلخ) عبر مصدرين:
   - **Exa**: محرك بحث ذكي (AI-native) يفهم معنى السؤال مو بس الكلمات.
   - **SearXNG**: محرك بحث ذاتي الاستضافة (مجاني بالكامل، يعمل على سيرفرنا).

2. **الفلترة (Filtering)** — يستبعد النتائج المكررة أو الضعيفة:
   - فحص تكرار حرفي (نفس الرابط).
   - فحص تكرار دلالي (نفس الخبر بصياغة مختلفة، عبر قاعدة بيانات متجهات Qdrant).
   - فحص جودة (تاريخ حديث، مصدر رسمي، محتوى كافٍ).

3. **الاختيار والصياغة (AI Model)** — نموذج Gemini من Google يقرأ المرشحين الناجين من الفلترة، يختار الأقوى والأكثر فائدة للمستخدم، يصنّفهم حسب المستوى (مبتدئ/متوسط/متقدم)، ويكتب لهم عنوان وملخص بالعربي بأسلوب تحريري ثابت.

4. **الحفظ والعرض (Enrichment)** — يجهّز كل خبر ببيانات إضافية (شعار الشركة، القطاع، الفئة)، يحفظه بملف JSON منظّم، وتعرضه الواجهة الأمامية.

## البنية التقنية

| الجزء | التقنية |
|---|---|
| الخلفية (Backend) | Python |
| الواجهة (Frontend) | HTML/JS ثابت (بدون framework) |
| قاعدة البيانات | PostgreSQL (النسخ/الإصدارات) + Qdrant (البحث الدلالي) |
| المصادقة | Keycloak |
| البحث | Exa API + SearXNG (self-hosted) |
| نموذج الذكاء الاصطناعي | Google Gemini (اختيار + صياغة + embeddings) |
| البنية التحتية | Docker Compose (كل خدمة بحاوية منفصلة) |

للتفاصيل التقنية العميقة، شوف [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).
