# تحديثات النظام — 2026-07-11 / 2026-07-12

توثيق لكل التعديلات اللي صارت بجلسة اليوم على pipeline الأخبار (اكتشاف/فلترة/اختيار Gemini)، بالترتيب الزمني، مع السبب وراء كل تعديل.

---

## 1. الانتقال من OpenAI إلى Gemini (3 نماذج حسب الدور)

**الملفات:** `backend/pipeline/modeling/gemini_client.py`, `backend/pipeline/modeling/model_client.py`, `backend/pipeline/modeling/selection.py`, `backend/pipeline/modeling/prompts/news_prompt.py`, `backend/.env`

- فصل الاختيار (selection) عن الصياغة العربية (rewrite) لطلبين منفصلين بدل طلب واحد مدمج — يسمح بموديل أرخص/أعلى حصة للصياغة (`gemini-flash-lite-latest`) وموديل أقوى للاختيار (`gemini-flash-latest`).
- إصلاح "قنبلة الطلبات": `model_allows_tool_record()` كان يستدعي Gemini **لكل أداة على حدة** داخل حلقة — تم تجميعه بدالة `model_allows_tool_records_batch()` تستدعي مرة وحدة للدفعة كاملة.
- إضافة `embed_texts()` بـGemini (`gemini-embedding-001`) لأول مرة — ما كانت موجودة أصلاً، الذاكرة الدلالية كانت OpenAI فقط.
- **إصلاح حرج:** `gemini-embedding-001` يرجع 3072 بُعد افتراضيًا، لكن قاعدة Qdrant مبنية على 1536 (من OpenAI) — كل عمليات حفظ الذاكرة كانت تفشل بخطأ "Vector dimension error". الحل: تحديد `output_dimensionality=1536` صراحة بطلب الـembedding.

---

## 2. إصلاح تكرار الجلب بين الدورتين (cycle 1 / cycle 2)

**الملف:** `backend/pipeline/fetching/news_discovery.py`

**المشكلة:** قائمة الأدوات المسجّلة (41) أصغر من حجم دفعة الاستعلام الافتراضية (48)، فكل دورة كانت تستعلم عن كل الـ41 أداة — يعني الدورة الثانية تكرر تقريبًا نفس نتائج الدورة الأولى، بدون أي فائدة إضافية حقيقية.

**الحل:**
- عمّمت دالة الدوران (`next_exa_tool_rotation_offset` → `next_tool_rotation_offset` عامة تقبل `state_key`).
- حجم دفعة كل دورة الحين يُحسب ديناميكيًا: `ceil(حجم_القائمة_الحالية / عدد_الدورات)` — يعني الدورة 1 تاخذ نصف الأدوات، الدورة 2 تاخذ النصف الثاني (تداخل بسيط بس).
- نفس المنطق طُبّق على SearXNG (كان يأخذ نفس أعلى 6 أدوات كل مرة بدون دوران إطلاقًا — الآن يدور بأوفست خاص فيه).

---

## 3. إصلاح تكرار الاستعلامات الثابتة بين الدورتين

**الملف:** `backend/pipeline/fetching/news_discovery.py`

**المشكلة (اكتُشفت بدليل حي):** استعلامات "عامة" ثابتة النص (9 استعلامات `EXA_PRODUCT_UPDATE_BROAD_QUERIES` + 10 استعلامات الطبقة العامة `GENERAL_NEWS_EXA_ROWS`) كانت تُعاد بالضبط بكل دورة — تأكدت من السجلات إن النتائج كانت **متطابقة حرفيًا** بالدورتين (نفس الروابط، نفس الترتيب).

**الحل:** أضفت باراميتر `cycle` يمر عبر السلسلة كاملة (`orchestrator.py` → `fetch_news_candidates` → `discovery_rows` → `exa_tool_update_script_rows`)، والاستعلامات الثابتة الحين تشتغل **بالدورة الأولى بس**.

---

## 4. تصحيح استعلامات الأدوات (Exa) — لغة أوسع + استبعاد صفحات المساعدة

**الملف:** `backend/pipeline/fetching/news_discovery.py`

كان الاستعلام `site:{domain} "{tool}" update` — كلمة وحدة ("update") بتحيّز نحو صفحات فهرسة "Release Notes" الطويلة (تُرفض لاحقًا كـ"roundup") وتفوّت إعلانات الإطلاق الحقيقية (مثال حي: فاتت إعلان GPT-5.6 الفعلي من OpenAI). الحل: توسيع الأفعال (`update OR launch OR launches OR introducing OR unveils OR announces OR "now available"`) + استبعاد `-site:help.{domain} -site:support.{domain}`.

**⚠️ محاولة تطبيق نفس الإصلاح على SearXNG فشلت وتم التراجع عنها** (انظر قسم 6).

---

## 5. توسيع قائمة الدومينات الموثوقة للطبقة العامة

**الملف:** `backend/pipeline/fetching/news_discovery.py`

قيد الدومينات الموثوقة (`TRUSTED_MEDIA_SOURCES`, `SAUDI_TRUSTED_MEDIA_DOMAINS`) كان يرفض ~44% من نتائج الطبقة العامة. وسّعت القائمتين بمصادر عالمية/إقليمية معروفة مؤكدة بتدقيق فعلي على بيانات تشغيل حقيقي (Wired, Ars Technica, MIT Tech Review, Reuters, Bloomberg... إلخ + مصادر سعودية إضافية).

---

## 6. محاولة تحسين استعلام SearXNG — فشلت وتم التراجع

**الملف:** `backend/pipeline/fetching/news_discovery.py`

جرّبت تطبيق نفس تحسين استعلام Exa (site: + استبعادات + عدة أفعال OR) على SearXNG. **النتيجة الفعلية: 23 من 24 استعلام رجعوا صفر نتائج** (تأكدت من `ai_updates_query_results.json`).

**السبب المرجّح:** SearXNG يكشط نتائج Google مباشرة (HTML scraping) وليس عبر API رسمي؛ استعلام طويل وغريب (site: + استبعادين + 7 بدائل OR) كافٍ يخلي Google يعامله كبوت ويرجّع صفحة فارغة/تحقق، عكس Exa اللي يستخدم API حقيقي يتحمّل هذا التعقيد بدون مشكلة.

**تم التراجع الكامل** لنفس الاستعلام البسيط المُختبر (`"{tool}" update`) اللي كان يرجّع نتائج فعلية (3-6 لكل رن). **درس مهم:** لا يمكن افتراض إن إصلاح ناجح لمصدر (API رسمي) سينجح تلقائيًا لمصدر آخر (كشط HTML) بدون اختبار مباشر.

---

## 7. رفع مهلة SearXNG (timeout)

**الملفات:** `backend/.env`, `searxng/settings.yml`

بعد رفع عدد استعلامات SearXNG لكل دورة (6 → 15)، صار عميلنا أحيانًا يطفش قبل ما يرد SearXNG (`Read timed out (read timeout=8)`). رفعت `AI_UPDATES_SEARXNG_TIMEOUT` من 8 إلى 16 ثانية، ورفعت `max_request_timeout` الداخلي لـSearXNG من 12 إلى 14 (عميلنا لازم يصبر أكثر من سقف SearXNG الداخلي).

---

## 8. حماية من التراجع عند الحفظ (Regression Guard)

**الملف:** `backend/pipeline/enrichment/news.py`

**حادثة حقيقية:** رصيد Exa نفذ أثناء رن خلفي تلقائي (background top-up)، فرجعت نتيجة أضعف (3 أخبار) بس الكود **استبدل** نتيجة أفضل محفوظة مسبقًا (8 أخبار) بدون أي مقارنة. الحل: `save_news_report()` الحين يقارن عدد الأخبار الجديد بالعدد المحفوظ حاليًا — لو الجديد أقل، يحتفظ بالمحفوظ ولا يستبدله (مع تسجيل تشخيصي واضح للتتبّع).

---

## 9. آلية "ضمان أقرب لـ12" — تخفيف قيد التنوع كملاذ أخير

**الملفات:** `backend/pipeline/modeling/selection.py`, `backend/pipeline/orchestrator.py`

بطلب صريح من المستخدم: لا توسيع لنافذة التاريخ (7 أيام تبقى صارمة)، بدلها تخفيف قيد "أقصى 2 عنصر لكل شركة" كملاذ أخير.

- `balance_for_diversity()` الحين تقبل `company_cap` بدل ثابت 2.
- `select_news_updates()`/`ask_model()` يعرّض `preselected_pool` (كل شي اختاره المودل قبل قيد التنوع، من كل الدورات) بدون أي طلب إضافي.
- بعد نهاية كل الدورات، لو العدد النهائي لسا أقل من الهدف: يُعاد بناء التنوع محليًا (بدون أي استدعاء API جديد) بقيد مخفف (2→4)، ويُستخدم بس لو تحسّن العدد فعليًا. العناصر الإضافية تُعلَّم `diversity_relaxed_fallback: true` للتتبّع.

**تحقّق حي:** رن فعلي أظهر `before: 7, after: 8` — الآلية تشتغل صح، بس أثبتت إن قيد التنوع مو السبب الرئيسي للنقص (باقي الفجوة عائدة لحجم الأخبار الحقيقية المستاهلة أسبوعيًا + تكرار الاستعلامات الثابتة، المعالج بقسم 3).

---

## 10. إصلاح فحص "هل الكورس عن AI فعلاً" (false positive)

**الملف:** `backend/pipeline/fetching/courses.py`

**حادثة حقيقية:** كورس "Digital Marketing Course" من HubSpot Academy (لا علاقة له بالـAI إطلاقًا) اجتاز فحص `has_ai` وظهر كـ"كورس AI متقدم". السبب: الفحص كان يبحث بكامل نص الصفحة المكشوطة (`page_text[:1600]`)، اللي غالبًا فيه روابط تنقّل جانبية لفئات أخرى (فيها كلمة "AI" بدون علاقة بالكورس الفعلي). الحل: تضييق الفحص للعنوان + المقتطف بس (`title + snippet`)، مو نص الصفحة الكامل.

---

## 11. توسيع مهلة انتظار الواجهة الأمامية

**الملف:** `frontend/News.html`

الواجهة كانت تنتظر 225 ثانية بس (150 محاولة × 1.5 ثانية) قبل ما تعرض "تعذر الإكمال" — بينما رن حقيقي كامل يوصل 650-1250 ثانية. رفعتها لـ1500 ثانية (25 دقيقة، ثابت واحد `GENERATOR_POLL_MAX_ATTEMPTS = 1000` بدل الرقم المكرر 3 مرات بالكود).

---

## 12. تعليقات تشخيصية إضافية (بدون تغيير سلوك)

- إضافة حقل `"content"` لملف `ai_updates_candidate_audit.json` — يخلي الملف نسخة مطابقة كاملة لما شافه المودل فعليًا (عنوان + مقتطف + كل الميتاداتا)، يسمح بإعادة اختبار نفس المدخلات على مودل ثاني للمقارنة.

---

## 13. إيقاف الدورات مبكرًا عند نفاد حصة الموديل

**الملف:** `backend/pipeline/orchestrator.py`

**حادثة حقيقية:** حصة Gemini اليومية نفذت تمامًا (0/120) أثناء اختبار حقيقي — الكود كان يكمل يجلب (Exa+SearXNG، ~90-150 ثانية) لكل دورة متبقية رغم إن أي استدعاء موديل تالٍ مضمون يفشل بنفس الخطأ. أضفت فحص `model_quota_remaining() <= 0` بعد كل دورة — لو الحصة خلصت، يتوقف فورًا وينتقل للحفظ بدل هدر وقت جلب بلا فائدة.

---

## 14. إثبات: الحماية من التراجع (قسم 8) تعمل صح تحت ظرف حقيقي

بعد نفاد حصة Gemini بالكامل أثناء رن اختباري حقيقي، النتيجة كانت `success: false, selected_count: 0, saved_news: false` — يعني الكود رفض يحفظ نتيجة فاشلة فوق أي محتوى سابق. لم يُختبر بعد سيناريو "نتيجة جزئية أضعف" (زي حادثة نفاد رصيد اكسا) بعد هذا الإصلاح تحديدًا، لكن نفس منطق الحماية (قسم 8) يغطيه.

---

## 15. تنظيف بنية الكود — منفَّذ

**الملاحظة كانت صحيحة 100%:** كان فيه بنيتان متوازيتان لنفس شغل الأخبار:
- `backend/pipeline/{fetching,filtering,modeling,enrichment}/news.py` — البنية الفعلية المستخدمة بكل مكان (كل استيراد بالكود الحي يشير لهذي).
- `backend/pipeline/news/{fetching,filtering,processing,tool_discovery}.py` — 4 ملفات صغيرة (30 سطر بالمجموع)، كلها مجرد إعادة تصدير بدون أي منطق خاص.

**تحققت:** بحثت بكل الكود عن أي استيراد لـ`backend.pipeline.news.*` — صفر نتائج. وأخطر من هذا: `backend/pipeline/news/tool_discovery.py` كان يستورد من `backend.pipeline.fetching.sources` — مسار مو موجود أصلاً (اتغيّر اسمه لـ`news_discovery.py` بمرحلة سابقة) — يعني **كان مكسور فعليًا لو حد حاول يستورده**.

**تم الحذف** (`git rm -r backend/pipeline/news/`) بعد تأكيدك الصريح — تحققت السيرفر الكامل يستورد بدون كسر بعدها.

---

## 16. إصلاح حرج: منعنا محتوى الأخبار من الوصول أصلاً (الملف: `news_discovery.py`)

**هذا على الأغلب السبب الرئيسي وراء توقف النتيجة عند 8 بدل 18.** طلب Exa الرئيسي للأدوات (المسار اللي يجيب 65-90% من كل المرشحين) كان مضبوط `"contents": {"text": False, "highlights": False}` — يعني **نطلب من Exa صراحة إنه ما يرجّع لنا أي نص فعلي للمقال**. المرشح يوصل للمودل بعنوان بس، بدون محتوى — فخطوة الصياغة العربية ترفضه بحق لأنها "ما تكفي تفاصيل تكتب بطاقة"، مو لأن الخبر سيء.

**الإصلاح:** `text: True, highlights: True` دايمًا. **تحقق حي:** نفس الاستعلام (Cursor) قبل الإصلاح رجّع محتوى فارغ، بعد الإصلاح رجّع 2200 حرف من نص المقال الحقيقي ("Today we are releasing Grok 4.5...").

---

## 17. تنظيف نهائي للملفات الضائعة

- حذفت `__pycache__/` اليتيمة لمجلد `backend/pipeline/news/` (كانت باقية بعد حذف ملفات .py، غير متتبعة بـgit أصلاً).
- حذفت `__pycache__/` بجذر المشروع (نفس الشيء — كل مجلدات `__pycache__` بالمشروع مستثناة من git و Docker عبر `.gitignore`/`.dockerignore`، مو مشكلة حقيقية للمشاركة/النشر، بس نظّفتها للترتيب المحلي).
- حذفت `test_results.txt` (بتأكيدك) — كان تقرير تصحيح قديم متروك بجذر المشروع، يذكر دوال محذوفة من الكود من مرحلة سابقة، بدون فائدة للمراجعة الحالية.
- **`Dockerfile` و`docker-compose.yml` بالجذر مقصودان، مو "ملفات ضايعة":** هذا الموضع القياسي اللي تتوقعه أدوات Docker تلقائيًا (`docker compose up` يبحث عن `docker-compose.yml` بالمجلد الحالي افتراضيًا). `docker-compose.yml` بالجذر رفيع جدًا (12 سطر) — بس يجمع الملفات التفصيلية الفعلية من `docker/compose/*.yml` عبر `include:`. التفاصيل كاملة بـ`docs/DOCKER_SETUP.md`.
- `CHANGELOG.md` (بالجذر) يوثّق إعادة هيكلة سابقة (قبل جلسة اليوم) — أبقيته لقيمته التاريخية، لكن التوثيق الحالي/النشط صار بـ`UPDATES.md`.

---

## 18. توحيد موديلات Gemini على `gemini-pro-latest` — ثم تراجع مؤكَّد بالدليل (نفس اليوم)

**الملفات:** `backend/pipeline/modeling/gemini_client.py`, `backend/.env`

كان الاختيار والصياغة على موديلين مختلفين بالقوة (`gemini-flash-latest` للاختيار، `gemini-flash-lite-latest` الأضعف/الأرخص للصياغة العربية تحديدًا). بقرار صريح من المستخدم بعد الانتقال لخطة مدفوعة: وحّدنا الاثنين على `gemini-pro-latest` — أقوى موديل متاح، خصوصًا للصياغة العربية.

**اكتشاف لاحق (نفس اليوم) غيّر القرار:** 3 تشغيلات حقيقية متتالية فشلت بـ`provider_quota_exhausted` خلال أول 2-4 طلبات، رغم عداد التتبع المحلي يظهر "109-113 من 120 متبقي". فحصت السبب بفحص مباشر لـGemini API (تجاوزًا لكودنا بالكامل) واتضح إن الرسالة الكاملة تقول:

```
Quota exceeded for metric: generativelanguage.googleapis.com/generate_content_free_tier_requests
limit: 0, model: gemini-3.1-pro
```

**`limit: 0` مو "خلصت الحصة" — هي "مافيه حصة أصلاً".** حصة الـfree tier لـ`gemini-3.1-pro` (اللي يوصّل له alias الاسم `gemini-pro-latest`) صفر على هذا المشروع تحديدًا، يعني **كل طلب لـPro بيفشل دائمًا 100%** بغض النظر عن الوقت أو عدد المحاولات — مو تذبذب أو حد يومي بينتظر ريست. Pro يتطلب حساب فوترة مربوط فعليًا بمشروع Google Cloud حتى يشتغل بأي حصة، وهذا غير مفعّل حاليًا رغم إن الخطة "مدفوعة" من ناحية القرار.

فحصت بنفس الطريقة باقي الموديلات على نفس المفتاح مباشرة: `gemini-flash-latest` ✅ نجح، `gemini-flash-lite-latest` ✅ نجح، `gemini-embedding-001` ✅ نجح — الثلاثة شغالين الآن فعليًا بدون فوترة.

**القرار النهائي (رجعنا لتقسيم الأدوار، مو نفس موديل واحد):**
- `GEMINI_SELECTION_MODEL=gemini-flash-lite-latest` — مرحلة ميكانيكية (فلترة/تصنيف)، الأرخص والأسرع كافي.
- `GEMINI_REWRITE_MODEL=gemini-flash-latest` — مرحلة الصياغة العربية الحساسة للجودة، أقوى من Lite ومضمون يشتغل الآن (بعكس Pro).
- `GEMINI_NEWS_MODEL=gemini-flash-latest` (متغيّر غير مستخدم فعليًا بأي مسار كود حاليًا — حُدّث للتناسق فقط).

**للمستخدم:** لو تبي فعليًا تستخدم `gemini-pro-latest`، الخطوة المطلوبة هي تفعيل الفوترة (Billing) على مشروع Google Cloud نفسه من [Google Cloud Console](https://console.cloud.google.com/billing) — مش شي أقدر أسويه بالكود، لأنه إعداد على مستوى الحساب/المشروع مو على مستوى الـAPI key.

## 19. دليل إضافي على أثر إصلاح المحتوى (قسم 16) — استعلامات حية لأدوات إضافية

بعد إصلاح `text: False → True`، سويت استعلامات Exa حية لـ14 أداة ماكانت بالعينة الأصلية (Spotify, Google Photos, Gemini API, Slack, Grammarly, GitHub Copilot...) للتحقق من الأثر الفعلي على أدوات مختلفة:

- **قبل الإصلاح (بيانات اليوم الأصلية):** كل مرشح من هذا النوع كان يوصل بمحتوى فارغ.
- **بعد الإصلاح:** من 14 أداة، لقينا **5 مرشحين حقيقيين مؤهلين فعليًا** (تحقق مباشر من المحتوى + التاريخ): Spotify Release Radar filters، Google Photos Video Remix، Gemini Study Notebooks، Gemini API Managed Agents، Slack Slackbot memory/MCP/voice — بالإضافة لـ2 من العينة الأولى (ElevenAgents Spotlight، M-Files AI Agents) = **7 من أصل ~20 أداة مُختبرة يدويًا** (نسبة تأهّل ~35%).

هذي النسبة (35%) أعلى بكثير من نسبة القبول اللي شفناها بالرنّات الفاشلة قبل إصلاح المحتوى (كانت شبه صفر لأن كل شي يوصل فارغ). **مؤشر قوي (مو ضمان) إن تشغيلة كاملة حقيقية على الـ41 أداة المسجّلة بعد الإصلاح ممكن توصل لعدد أقرب من 12-18** — بس ده لسا يحتاج تأكيد من تشغيلة Gemini فعلية كاملة (محاولات اليوم كلها ضربت حدود حصة حقيقية من Google، مو خلل بالكود — راجع القسم التالي).

## 20. محاولات تشغيل حقيقي بعد إصلاح المحتوى — من "حصة مستنفدة" إلى السبب الجذري الحقيقي

بعد إصلاح المحتوى (قسم 16)، حاولت 3 تشغيلات حقيقية كاملة (`run_pipeline`) للتحقق من الأثر الفعلي على العدد النهائي. الثلاثة فشلوا بـ`provider_quota_exhausted` بعد `gemini-pro-latest` بالتحديد (طلب رقم 7، ثم 9، ثم 11/13 حسب المحاولة) — في البداية فسّرتها كحد يومي حقيقي من Google بينتظر ريست. **فحص مباشر لاحق لـGemini API (قسم 18) كشف إنها مو حد يومي — هي `limit: 0` ثابت لموديل Pro تحديدًا على هذا المشروع لعدم تفعيل الفوترة، يعني كل طلب لـPro كان سيفشل دائمًا بغض النظر عن الوقت.**

بعد إرجاع `GEMINI_SELECTION_MODEL`/`GEMINI_REWRITE_MODEL` لموديلات Flash (مؤكَّدة شغالة بفحص مباشر) وإعادة بناء الحاوية (`docker compose up -d --build`)، شغّلت تشغيلة حقيقية كاملة إضافية (`run_id: full-20260712T013103Z-0f3762d5`) للتحقق من الوصول للعدد المستهدف.

**النتيجة: نجاح كامل، أول تأكيد رقمي حقيقي (مو محاكاة، مو يدوي) بعد كل إصلاحات الجلسة:**

```
pipeline.finished | success: true, selected_count: 18, saved_news: true, total_seconds: 682.6
```

- **الأخبار: 18/18 — بالضبط 6/6/6 على المستويات الثلاثة (Beginner/Intermediate/Advanced).** هذا الحد الأقصى المطلوب، مو الحد الأدنى (12).
- **الكورسات: 6** (2 Beginner، 2 Intermediate، 1 Advanced، 1 بدون تصنيف مستوى — راجع القسم "مشاكل معروفة" تحت).
- **الأفلام: 5** محفوظة.
- `news.json` انحفظ تلقائيًا من داخل الـpipeline نفسه (`save_news_json.finished`) — مو حفظ يدوي مني، ومر من حارس التراجع (قسم 8) بدون أي تدخل لأن النتيجة كانت أفضل من الموجود أصلاً.
- التوكنز الفعلية لهذي التشغيلة هي الأساس اللي أُعيد عليه حساب التكلفة بـ`docs/GEMINI_MODEL_AND_BUDGET.md` (≈$0.09/تشغيلة بموديلات Flash).

**هذا يغلق الهدف المفتوح من القسم 19 — إصلاح `contents.text` (قسم 16) فعليًا أوصل لعدد حقيقي كامل بدون أي تدخل يدوي، بمجرد ما الموديل صار شغال فعليًا.**

---

## مشاكل معروفة لسا مفتوحة (لم تُحل بعد)

- **الكورسات:** نقص بالعرض العام (2/مستوى مقابل هدف 6/مستوى) — يحتاج توسعة اكتشاف مصادر، لم يُعالج اليوم.
- **الكورسات — تصنيف مستوى فارغ + محتوى منتهي الصلاحية:** بالتشغيلة الحقيقية بقسم 20، كورس واحد من أصل 6 ("برنامج إكسنتشر في الذكاء الاصطناعي - AI Bridge") انحفظ بـ`level: ""` (بدون تصنيف)، ومحتواه الفعلي يقول "تم انتهاء فترة التسجيل بالبرنامج" — يعني صفحة تسجيل منتهية الصلاحية مرّت من الفلترة رغم إنها مو كورس متاح فعليًا للمستخدم. يحتاج فحص أعمق لمنطق تصنيف المستوى بـ`courses.py`/`supporting.py` (ليش رجع فاضي؟) وربما فلتر إضافي يكشف عبارات "انتهت فترة التسجيل"/"registration closed" قبل القبول. لم يُعالج اليوم — مرصود فقط.
- **SearXNG:** مساهمته النهائية بالأخبار تبقى هامشية (3-6 عنصر لكل رن) رغم كل الإصلاحات — نمط بحثه الحرفي (keyword) عبر كشط HTML أضعف بطبيعته من البحث الدلالي لـExa، وأي محاولة لتقويته بمزيد من التعقيد تخاطر بكسره كليًا (انظر قسم 6).
- **الضمان الرياضي لـ12 خبر:** لا يوجد — يعتمد على حجم الأخبار الحقيقية المستاهلة أسبوعيًا. الآليات المطبّقة (تنوّع مرن، منع تكرار الاستعلامات) تقرّب من الهدف بدون التضحية بالجودة، لكن لا تضمنه رياضيًا في كل الأسابيع.
